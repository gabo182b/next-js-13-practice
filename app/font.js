import { Space_Grotesk as spaceGrotesk } from 'next/font/google'

// new compoonent for font managing
export const font = spaceGrotesk({
  weight: ['400', '700'],
  subsets: ['latin'],
  variable: '--font-grotesck'
})
